## Rapport — Mini Projet DevOps

**Nom & Prénom :** …

### 1) Repository GitHub
- **Lien du repo**: …
- **Structure du projet**: (capture)
- **Historique des commits**: (capture)
- **Branches `main` et `dev`**: (capture)
- **Pull Request `dev -> main`**: (capture)

### 2) GitHub Actions
- **Workflow**: `.github/workflows/ci.yml` (capture du fichier ou extrait lisible)
- **Déclenchements**: `push` et `pull_request` (capture)
- **Exécution réussie/échouée**: (captures + explications si échec)

### 3) Jenkins
- **Job**: `PipeLine-NomPrenom` (capture config + pipeline run)
- **SCM/GitHub**: (capture)
- **Pipeline script**: `Jenkinsfile` (extrait lisible)
- **Vue personnalisée** (affiche les jobs suffixés `PipeLine`) : (captures)
- **Exécution du pipeline**:
  - Checkout (capture)
  - Build/Test (capture)
  - Archive (capture)
  - Deploy (capture si Docker OK, sinon expliquer)

### 4) Docker / docker-compose
- **Dockerfile**: (capture/extrait)
- **docker-compose.yml**: (capture/extrait)
- **Preuve de build/run**: logs + capture navigateur `http://localhost:8080/`

### 5) Slack
- **Configuration Jenkins + Slack**: (capture)
- **Notifications**: succès/échec (captures)

### 6) Conclusion
- Problèmes rencontrés + solutions
- Améliorations possibles

